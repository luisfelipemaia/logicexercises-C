#include <stdio.h>
#include <stdlib.h>

int main ( void ) {
    int n1;
    int n2;
    int multp2;
    int multp3;
    int multp4;

    printf("N1: ");
    scanf("%d", &n1);

    printf("N2: ");
    scanf("%d", &n2);

    if(n1 < n2) {
        for(int i = n1; i <= n2; i++){
            if(){

            }
            if(){

            }
            if(){

            }
        }
    } else {
         for(int i = n2; i >= n2; i--) {
            if(i % 2 == 0) {
            
            }
            if(){

            }
            if(){

            }
        }
    }

    return 0;
}